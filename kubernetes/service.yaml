apiVersion: v1
kind: Service
metadata:
  name: social-frontend
  namespace: social-platform
  labels:
    app: social-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: social-frontend
